// Game Master Icon Types
// This enum defines the different types of icons used in the Game Master interface

export enum GMIconType {
  CHARACTER = 'character',
  GAME_OBJECT = 'gameobject',
  ITEM = 'items',
  MAP = 'map',
  SKILL = 'skill'
}

// Export the enum as default for easier imports
export default GMIconType;